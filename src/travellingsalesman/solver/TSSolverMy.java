package travellingsalesman.solver;

public class TSSolverMy extends TSSolverBase{
    public TSSolverMy(int clusterCount, int maxDeep, int maxNodesInCluster) {
        super(clusterCount, maxDeep, maxNodesInCluster);
    }
}
